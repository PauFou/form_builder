# PostgreSQL Client Authentication Configuration for CI
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Trust all connections from localhost and Docker network
# This is safe for CI environments but should NEVER be used in production
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust
host    all             all             0.0.0.0/0               trust